# Voice rules — canonical copy lives elsewhere

The full voice ruleset (kill list, register, em-dash ban, operator-vocabulary
ban, quote rule, polish rule, the two tests) is in:

`~/projects/hermes-outreach/docs/haythams-voice.md`

That file is the single authoritative version. This reference used to hold a
verbatim duplicate; loading both wasted ~4KB of context per session with zero
information gain. Read the doc above once per session when drafting — do not
look for the rules here.
